import * as React from 'react';

interface ICreatePostProps {
}

const CreatePost: React.FunctionComponent<ICreatePostProps> = (props) => {
  return <div>Crate Post</div>;
};

export default CreatePost;
